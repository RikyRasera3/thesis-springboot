spring:
  datasource:
    url: "jdbc:postgresql:///${DB_NAME}?cloudSqlInstance=${INSTANCE_CONNECTION_NAME}&socketFactory=com.google.cloud.sql.postgres.SocketFactory"
    username: ${DB_USER}
    password: ${DB_PASSWORD}

logs:
  enabled: ${LOGS_ENABLED:false}

logging:
  level:
    com.google.cloud.sql: DEBUG