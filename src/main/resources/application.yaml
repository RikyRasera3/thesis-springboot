spring:
  application:
    name: springboot
  jpa:
    open-in-view: false
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
        default_schema: thesis
        show_sql: false
